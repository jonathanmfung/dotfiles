# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Look into using this
# more elegant way of doing proper exec
#set $exec exec --no-startup-id

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
# font pango:Roboto 12px
# font pango:SourceSansPro, 12
# font pango:SourceCodePro 12
# font -ADBO-Source_Code_Pro-normal-normal-normal-*-*-*-*-*-m-0-iso10646-1

# this is hacky to change window title font as the previous lines don't work (3/13/21)
for_window [class=".*"] title_format "<span font='Source Sans Pro 12'>%title</span>"

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
# font pango:DejaVu Sans Mono 8

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork

# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
exec --no-startup-id nm-applet

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status

# bindsym XF86AudioRaiseVolume exec --no-startup-id amixer sset Master 1%+
# bindsym XF86AudioLowerVolume exec --no-startup-id amixer sset Master 1%-
# bindsym XF86AudioMute exec --no-startup-id amixer sset Master toggle

bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +2% && $refresh_i3status && notify-send 'Volume up'
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -2% && $refresh_i3status && notify-send 'Volume down'
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status && notify-send 'Mute/Unmute'
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

bindsym XF86AudioPlay exec --no-startup-id playerctl -p spotify play-pause && $refresh_i3status && notify-send 'Play/Pause'
bindsym XF86AudioPrev exec --no-startup-id playerctl -p spotify previous && $refresh_i3status && notify-send 'Prev Track'
bindsym XF86AudioNext exec --no-startup-id playerctl -p spotify next && $refresh_i3status && notify-send 'Next Track'

# Sreen brightness controls
bindsym XF86MonBrightnessUp exec xbacklight -inc 2 # increase screen brightness
bindsym XF86MonBrightnessDown exec xbacklight -dec 2 # decrease screen brightness

# mpc/mpd/ncmpcpp controls
# -p 6605 is the port that mpd/ncmpcpp is currently configed to, defualt is 6600
bindsym $mod+n exec mpc -p 6605 prev
bindsym $mod+m exec mpc -p 6605 toggle
bindsym $mod+comma exec mpc -p 6605 next

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
# bindsym $mod+Return exec --no-startup-id alacritty
# bindsym $mod+Shift+Return exec --no-startup-id alacritty --class floating # -d 80 35 
bindsym $mod+Return exec --no-startup-id termite --title=termite
bindsym $mod+Shift+Return exec --no-startup-id termite --class=floating 
#--position 3000 450 
#i3-sensible-terminal

# open an empty window
# This is an unsupported i3 feature, only for testing purposes
bindsym $mod+ctrl+space open

# start a floating emacsclient
bindsym $mod+ctrl+Return exec emacsclient -c -F '((width . 200) (height . 60))'
bindsym $mod+ctrl+Shift+Return exec emacsclient -c -F '((name . "floating")(width . 200) (height . 60))'
#--class floating

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
bindsym $mod+d exec --no-startup-id dmenu_run -nb '#707070' -sf '#ffffff' -sb '#00BFE0' -nf '#ffffff' -x 450 -b -y 4 -w 900
# -x 450 -b -y 3 -w 900
# -x 590 -b -y 3 -w 800

# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# control mouse poniter
bindsym $mod+ctrl+u          exec --no-startup-id xdotool click 1
bindsym $mod+ctrl+i          exec --no-startup-id xdotool click 3

bindsym $mod+ctrl+h          exec --no-startup-id xdotool mousemove_relative -- -50 0
bindsym $mod+ctrl+j          exec --no-startup-id xdotool mousemove_relative 0 50
bindsym $mod+ctrl+k          exec --no-startup-id xdotool mousemove_relative 0 -50
bindsym $mod+ctrl+l          exec --no-startup-id xdotool mousemove_relative 50 0

# Make the currently focused window a scratchpad
bindsym $mod+Shift+minus move scratchpad

# Show the first scratchpad window
bindsym $mod+minus scratchpad show


# split in horizontal orientation
bindsym $mod+b split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# switch between last visited workspace
bindsym $mod+Tab workspace back_and_forth

# switch between next/prev workspaces on current monitor
bindsym $mod+ctrl+Shift+h workspace prev_on_output
bindsym $mod+ctrl+Shift+l workspace next_on_output

# focus relative monitors/outputs
bindsym $mod+ctrl+Shift+j focus output down
bindsym $mod+ctrl+Shift+k focus output right


# focus the child container
#bindsym $mod+d focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"


# Pulls up floating terminals: file manager, calender, networkmanager
#bindsym $mod+i exec --no-startup-id alacritty --class floating -d 120 40 --hold -e ranger
bindsym $mod+i exec emacsclient -c -F '((name . "floating"))' -e "(dired nil)"
# --position 2800 400 
#bindsym $mod+o exec --no-startup-id alacritty --class floating -d 22 25 --hold -e cal -3
#bindsym $mod+o exec emacsclient -c -F '((name . "floating"))' -e "(calendar)"
bindsym $mod+o exec notify-send "$(cal)"
# --position 3500 400 
bindsym $mod+p exec --no-startup-id alacritty --class floating -d 80 40 --hold -e nmtui
# --position 3200 400 

# floating pavucontrol to fix audio configurations
bindsym $mod+u exec --no-startup-id pavucontrol --class floating

bindsym $mod+Shift+s exec --no-startup-id spotify --disable-gpu 

for_window [class="Alacritty" instance="floating"] floating enable border pixel 3
for_window [class="floating"] floating enable border pixel 3
# Used for emacsclient keybind
for_window [title="floating"] floating enable border pixel 3

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 5 px or 5 ppt
        bindsym j resize grow height 5 px or 5 ppt
        bindsym k resize shrink height 5 px or 5 ppt
        bindsym l resize grow width 5 px or 5 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
# bar {
#        status_command i3status
#	mode dock
#	position bottom
#	output primary
#	tray_padding 5
#
#	font pango:Source Code Pro 10
#	separator_symbol "||"
#	height 20
#
#	colors {
#		background #ffffff
#		statusline #000000
#		separator #666666
#
#		focused_workspace  #00BFE0 #00BFE0 #ffffff
#		active_workspace   #333333 #5f676a #ffffff
#		inactive_workspace #707070 #000000 #888888
#		urgent_workspace   #2f343a #900000 #ffffff
#		binding_mode       #2f343a #900000 #ffffff
#    	}
#}

# Start of MY configs

# You can also use any non-zero value if you'd like to have a border
for_window [class=".*"] border pixel 5 # 3

gaps inner 20  #50 # 15 10
gaps outer 5 #100 # 0
gaps bottom 33 # 23 13

font pango:Source Mono Pro 16  #10
title_align center

#smart_gaps on
#smart_gaps inverse_outer 
#smart_borders on
#hide_edge_borders smart_no_gaps

exec firefox
exec spotify --disable-gpu
# running systemd user service for emacs daemon doesn't work
exec emacs --daemon && exec emacsclient -c -F '((width . 200) (height . 60))'
exec nitrogen --restore &
exec sudo ntpdate -s time.nist.gov
exec mpd
#exec anki
exec pulseeffects --gapplication-service

# class                 border  backgr. text    indicator child_border
#client.focused          #ffffff #ffffff #000000 #2e9ef4   #00BFE0
client.focused          #ffffff #ffffff #000000 #80d2e0   #00BFE0
client.focused_inactive #ffffff #ffffff #000000 #484e50   #5f676a
client.unfocused        #707070 #707070 #ffffff #292d2e   #707070

#FF7C00 #707070 #00BFE0
#client.focused          #FF7C00 #707070 #ffffff #2e9ef4   #00BFE0
#client.focused_inactive #333333 #5f676a #ffffff #484e50   #5f676a
#client.unfocused        #707070 #000000 #ffffff #292d2e   #707070


# exec --no-startup-id redshift -v -r -t 4000:3000 -l 37.77:-122.42 &!

# 4k
#bindsym $mod+t exec xrandr --output eDP1 --primary --mode 1920x1080 --pos 1975x2160 --rotate normal --output DP1 --mode 1920x1080 --pos 0x240 --rotate right --output HDMI1 --mode 3840x2160 --pos 1080x0 --rotate normal --output HDMI2 --off --output VIRTUAL1 --off
# 1440p
bindsym $mod+t exec xrandr --output eDP1 --primary --mode 1920x1080 --pos 1400x1623 --rotate normal --output DP1 --mode 1920x1080 --pos 0x0 --rotate right --output HDMI1 --mode 2560x1440 --pos 1080x183 --rotate normal --output HDMI2 --off --output VIRTUAL1 --off
bindsym $mod+y exec xrandr --output HDMI1 --off  --output DP1 --off

workspace 1 output HDMI1
workspace 2 output HDMI1
workspace 3 output HDMI1
workspace 4 output eDP1
workspace 5 output eDP1
workspace 6 output eDP1
workspace 7 output DP1
workspace 8 output DP1
workspace 9 output DP1

# Force applications to start on desired workspaces
assign [class="^Spotify$"] $ws4
#assign [title="Firefox$"] $ws2
# assign [class="^Emacs$"] $ws1

# Custom keybindings
bindsym $mod+c move workspace to output right
bindsym $mod+shift+v move workspace to output up
bindsym $mod+z exec flameshot gui 
bindsym $mod+shift+z exec i3lock -c 707070 
bindsym $mod+shift+f exec firefox -new-window
#bindsym $mod+shift+d fullscreen toggle global # fullscreens across whole virtual x display


#toggle light and dark terminal scheme
bindsym $mod+shift+t exec wal -n --saturate 0.8 -i ~/Downloads/walls/Jack_Vanzet_68.jpg -l -e
bindsym $mod+shift+y exec wal -n --saturate 0.8 -i ~/Downloads/walls/waitme.jpg -e

exec_always --no-startup-id $HOME/.config/polybar/launch.sh
exec_always --no-startup-id picom -b 
